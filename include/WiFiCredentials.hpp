#pragma once

/*
BEFORE EDITING THIS FILE
----------------------------
Make sure to run the following command from the repo root
git update-index --assume-unchanged include/WiFiCredentials.hpp
in order to prevent your WiFI credentials from being published online
*/
// WiFi network name and password:
const char* netName = "WIFI_NAME";
const char* netPswd = "********";
